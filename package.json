{
  "name": "vecgostov",
  "version": "1.0.0",
  "private": true,
  "description": "Več gostov — websites, Google Maps and direct booking for holiday-rental owners in Slovenia.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/dev.mjs",
    "preview": "node scripts/build.mjs && node scripts/serve.mjs",
    "images": "node scripts/images.mjs",
    "check": "node scripts/build.mjs && node scripts/check.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "sharp": "^0.35.4"
  }
}
